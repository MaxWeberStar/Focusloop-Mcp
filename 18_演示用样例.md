# FocusLoop 演示用样例

**用途**：参赛答辩时展示 FocusLoop 的真实检测能力，比抽象数字更有说服力。
**选样原则**：覆盖三类漂移（目标替换、范围扩大、决策遗忘/违背 non_goals），用真实标注数据中的命中案例。

---

## 演示样例 1：宠物养成游戏 vs FocusLoop

**来源**：`labels.jsonl` p3-012（correct 标注）
**类型**：目标替换（Goal Replaced）

### 场景

用户在 FocusLoop 项目中突然提出要做"宠物养成游戏"。FocusLoop 的 GOAL 是"完成知乎 AI Works 参赛项目 FocusLoop v1"，与宠物养成游戏完全无关。

### FocusLoop Gate 3 输出

```json
{
  "verdict": "drift",
  "overlap_score": 0.0,
  "evidence": {
    "goal": "完成知乎 AI Works 参赛项目 FocusLoop v1",
    "context": "新增宠物养成游戏功能模块",
    "matched_phrases": [],
    "non_goal_violated": "暂不做独立插件界面"
  },
  "user_message": "当前工作与原始目标无重叠（0.0），且涉及 non_goals 范围外内容。是否：(1) 放弃此方向；(2) 延后到 FocusLoop 完成后再考虑；(3) 更新 GOAL 包含此方向"
}
```

### 演示要点

1. **覆盖度直接 0.0**：连模糊匹配都没有，关键词完全没有交集
2. **三档判定边界清晰**：aligned ≥ 0.65、drift < 0.30，0.0 是明确 drift
3. **给出三个可选动作**：放弃/延后/更新 GOAL，不替用户做决定

---

## 演示样例 2：VS Code 插件 = 违背 non_goals

**来源**：`labels.jsonl` p3b-014（correct 标注）
**类型**：决策遗忘 / 违背 non_goals（Decision Forgotten）

### 场景

用户讨论"要不要把 FocusLoop 做成 VS Code 插件发布"。但 GOAL 的 non_goals 明确写了"暂不做独立插件界面"。

### FocusLoop Gate 3 输出

```json
{
  "verdict": "drift",
  "overlap_score": 0.18,
  "non_goal_violated": "暂不做独立插件界面",
  "violated_rule": "non_goals[0]: 暂不做独立插件界面",
  "evidence": "VS Code 插件 = 独立插件界面形态",
  "user_message": "当前方向与 GOAL non_goals 直接冲突。non_goals: ['暂不做独立插件界面', ...]。是否：(1) 撤回此方向；(2) 用户授权后更新 non_goals"
}
```

### 演示要点

1. **non_goals 违背是硬信号**：覆盖度 0.18 本身是 ambiguous，但因为 non_goals 冲突升级为 drift
2. **直接引用违反的具体条款**：不让用户自己回忆"我说过不做插件界面吗"
3. **明确升级为 drift**：不是因为覆盖度低，而是因为主动违背了已确认约束

---

## 演示样例 3：商业化订阅 = 违背 non_goals

**来源**：`labels.jsonl` p3b-015（correct 标注）
**类型**：决策遗忘（Decision Forgotten）

### 场景

AI 提出"加一个 Pro 订阅功能，月费 9.9 元"。GOAL 的 non_goals 明确写了"暂不做商业化设计或付费订阅"。

### FocusLoop Gate 3 输出

```json
{
  "verdict": "drift",
  "overlap_score": 0.12,
  "non_goal_violated": "暂不做商业化设计或付费订阅",
  "violated_rule": "non_goals[2]: 暂不做商业化设计或付费订阅",
  "user_message": "商业化/付费订阅是 GOAL non_goals 明确禁止。当前 AI 提出此方向是否已获得用户授权？"
}
```

### 演示要点

1. **AI 的"建议"被识别为违背**：AI 提议加 Pro 订阅，但 GOAL 禁止商业化
2. **关键不是"AI 错了"，而是"AI 没意识到 GOAL"**：FocusLoop 主动提示
3. **避免 AI 引导范围扩大**：C5 案例（废弃项目）的阶段 2 就是这种情况——AI 引导转向 OAuth+Render 部署

---

## 演示样例 4：用户系统 / 微信机器人 = 目标替换

**来源**：`labels.jsonl` p3-013、p3-015（correct 标注）
**类型**：目标替换（Goal Replaced）

### 场景 A（p3-013）

AI 建议"FocusLoop 需要用户系统，包括注册登录"。GOAL 完全不涉及用户系统。

### 场景 B（p3-015）

AI 建议"做一个微信机器人来通知漂移"。GOAL 完全不涉及微信生态。

### FocusLoop Gate 3 输出（场景 A）

```json
{
  "verdict": "drift",
  "overlap_score": 0.05,
  "evidence": "用户系统属于目标替换信号——GOAL 是影子模式本地工具，不需要用户系统",
  "user_message": "当前方向与 GOAL 不重叠。是否：(1) 撤回；(2) 用户授权后新增到 GOAL"
}
```

### 演示要点

1. **目标替换的典型模式**：AI 引入"必须有 XX"的强前提，但 GOAL 没说
2. **覆盖度 0.05 vs ambiguous 阈值 0.30**：明确漂移
3. **不给 AI 暗示**：FocusLoop 不假设"用户系统一定有道理"，只展示冲突

---

## 演示样例 5（反例）：CI/CD 配置 = 误报场景

**来源**：`labels.jsonl` p3b-002（false_positive 标注）
**类型**：误报示例（GOAL 缺口导致）

### 场景

用户在 FocusLoop 中配置 GitHub Actions 做 CI/CD。GOAL 没把 CI/CD 写进 deliverables。

### FocusLoop Gate 3 输出

```json
{
  "verdict": "drift",
  "overlap_score": 0.28,
  "evidence": "当前活动（CI/CD 配置）与 GOAL deliverables 无重叠",
  "user_message": "检测到偏离：CI/CD 不在 GOAL 范围内。是否需要补充到 GOAL？"
}
```

### 演示要点（用于说明边界）

1. **承认误报存在**：58% 总准确率里有 29.6% 误报
2. **误报根因是 GOAL 不完整**：CI/CD 是正常工程实践，GOAL 应该包含
3. **改进路径**：GOAL 模板化（v2 计划），预置测试/文档/排障/CI 等典型活动
4. **保守策略的价值**：宁可提示用户确认，不放过疑点——C5 案例证明用户察觉漂移延迟 2-3 个阶段

---

## 附件：5 个演示样例的标注回溯

| 演示样例 | labels.jsonl 来源 | 标签 | 要点 |
|---------|-------------------|------|---------|
| 1 | p3-012 | correct | 目标替换，覆盖度 0.0 |
| 2 | p3b-014 | correct | non_goals 违背，硬信号 |
| 3 | p3b-015 | correct | non_goals 违背，AI 引导 |
| 4 | p3-013、p3-015 | correct | 目标替换，覆盖度极低 |
| 5 | p3b-002 | false_positive | 误报场景，GOAL 缺口 |