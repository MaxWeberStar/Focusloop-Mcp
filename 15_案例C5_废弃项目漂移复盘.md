# 案例 C5：某论坛问答帖检索的网页产品项目-五阶段对话复盘

**状态**：作为 FocusLoop PRD 的市场问题证据。
**日期**：2026-09-21
**用途**：说明「AI 长程开发中的目标漂移问题」是真实存在的，且与 FocusLoop 三类漂移（目标替换、范围扩大、决策遗忘）模式高度吻合。

---

## 0. 脱敏说明

- 项目名 → 「论坛问答帖的搜索深度+分类功能产品」
- 作者署名 → David
- 项目代号：「F」

---

## 1. 项目目标（初始）

**核心目标**：对某平台高赞问答帖做检索 + LLM 分析，按时间维度聚合分类，呈现「时间轴 / 演进卡片 / 观点谱系」三个功能。

**MVP 设想**（用户最初表述）：搜索 → 聚类 → 自动分类 → UI 呈现最小可用版本。

**为什么这个案例对 FocusLoop 有价值**：
- 是真实的多阶段 AI 协作开发，不是合成场景
- 用户最终主动放弃，原因不是技术失败，而是路线漂移导致产品形态偏离初始设想
- 整个过程的对话日志完整可追溯

---

## 2. 五阶段时间线与漂移节点

| 阶段 | 用户实际工作 | 偏离信号 | 漂移类型 |
|------|------------|---------|---------|
| **阶段 1** | 完成初版 Demo，时间轴+卡片可演示 | — | 无漂移 |
| **阶段 2** | 转向 OAuth + Render 公网部署 | 从「产品迭代」切换到「基础设施验证」 | **范围扩大**（引入交付硬门槛） |
| **阶段 3** | AI 把方向带向「用户自行打标签后再分类」的结构化回答分析 | 产品形态从「自动时间轴」变成「用户标注工具」 | **目标替换**（核心机制被改写） |
| **阶段 4** | 用户复盘："焦点不在这里了，这不像是为了作出产品，而是为了交任务" | 用户语气从建设性转为疲惫 | **决策遗忘**（产品价值定义被遗忘） |
| **阶段 5** | 完全重启，迁移到新工作目录，重做 PRD/TDD | 工作环境、文档系统全部推倒重来 | **用户授权的目标演进**（合法重置） |

---

## 3. 三类漂移的具体证据（脱敏后）

### 漂移 1：范围扩大（阶段 2）

**原始范围**：完成初版 Demo 并优化。
**实际执行**：转向 OAuth 接入 + Render 公网部署。

**触发机制**（对话原话还原）：
> "Agent 上我需要新开会话先做什么？"
> AI 回复："必须先打通 OAuth，否则提交时评委点进去发现不能登录，直接扣完成度分。"

**FocusLoop 对照**：
- Gate 1 信号：交付物类型从「Demo 优化」新增「公网部署 + OAuth」
- 当时用户已读到「提交硬门槛」的引导，但没有质疑"这个硬门槛是否属实"
- 若当时有 FocusLoop，应在 AI 提出 OAuth 必要性时打断一次确认

**事后验证**：阶段 4 用户复盘时承认"上线不是终点，是起点；提交前 OAuth 不是真正的硬门槛"——阶段 2 的范围扩大基于错误前提。

### 漂移 2：目标替换（阶段 3）

**原始核心机制**：LLM 自动分类（用户原话：「答案太多可以先做聚类或主成分分析筛选、再搜索证据做材料支持、最后概要打标签写关键词」）。
**实际产出**：5 条材料的「分组、筛选、并列比较」界面，用户需要先展开摘要、查看原文，才能在比较区做合并阅读。

**关键转折**：阶段 3 的产出物要求"用户对检索到的回答自行打标签后再分类"，这与 MVP 设想的"自动分类"完全相反。

**FocusLoop 对照**：
- Gate 1 信号：核心机制从「LLM 自动分类」变成「用户手动标注+AI 比较」
- 这不是技术失败，而是产品形态从「自动分析工具」变成「人工协作工具」
- 若当时有 FocusLoop，应在用户多次表达"用户必须自行打标签"时触发"核心机制变更"确认

**事后验证**：用户在阶段 4 明确放弃这个产品方向，承认"不能再编造因果关系是正确的；但从不能证明演进直接走到做摘要比较页，中间还需要你确认产品边界"。

### 漂移 3：决策遗忘（阶段 4）

**已被遗忘的决策**：
- 阶段 1 已确定核心机制是"时间轴+自动分类"
- 阶段 1 已确定产品价值是"帮助读者理解观点怎样被补充、质疑和修正"
- 阶段 1 已确定验收标准是"读者能体验到观点的演进过程"

**实际行为**：阶段 2-3 期间多次转向"能否登录""能否搜索""能否上线"，每次都未回到原始价值定义问"这如何服务于读者体验观点演进"。

**FocusLoop 对照**：
- Gate 3 信号：实施计划与原始路线重叠度持续下降
- 若当时有 FocusLoop，应在每次阶段切换时计算「该阶段产出如何服务于原始价值」并提示用户确认

**用户原话证据**：
> "我的感受是：慢慢的我的焦点就不在这里了，这不像是为了作出产品，而是为了交任务，尽管节点很重要"

这是**目标漂移已经发生且用户意识到**的明确信号——但发生时机是漂移 2-3 个阶段之后。

### 漂移 4：用户授权的演进（阶段 5）—— 非漂移

**用户行为**：完全重启项目，迁移到新工作目录，重做 PRD/TDD。
**这是合法演进**：用户在阶段 4 明确复盘后，主动决定"重新开始"。

**FocusLoop 对照**：
- 这是用户在发现漂移后**主动修复**，不是漂移本身
- FocusLoop 应区分：用户授权的目标重置 vs AI 引导的目标替换
- 关键差别：阶段 5 用户带着"基于阶段 4 复盘的明确决策"重启；阶段 2-3 是"被引导而未察觉"

---

## 4. FocusLoop 在此案例中本可以做什么

按 PRD 第 5 节需求映射：

| 漂移节点 | FocusLoop 应做的事 | 实际未做的事 |
|---------|-------------------|-------------|
| 阶段 2 转向 OAuth | Gate 1：询问"新增交付物是否服务于原始价值" | AI 直接给建议，未确认 |
| 阶段 3 核心机制改写 | Gate 1：标记「用户标注工具」与「自动分析工具」机制不兼容 | 用户自己接受了 AI 的设计建议 |
| 阶段 4 用户语气转变 | Gate 3：每次阶段切换提示"该阶段产出与原路线重叠度" | 无提示机制 |
| 阶段 5 重启 | Gate 1：识别为用户授权演进，正常处理 | （无对照，因为该案例未使用 FocusLoop）|

---

## 5. 案例对 PRD 的影响

**作为 C4（项目自检案例）的扩展证据**：
- C4 描述的是"路线衔接表达不清"
- C5 描述的是"漂移确实在真实项目中发生，且未被及时察觉"

**对市场报告的修正**：
- 01 市场报告中的「市场空白」假设得到部分验证：长程 AI 协作中的路线聚焦确实存在市场需求
- 但 C5 证明用户察觉漂移的时机通常滞后 2-3 个阶段，FocusLoop 的价值在于"主动发现"

**对 PRD 范围的影响**：
- 案例验证了"用户授权的目标演进"和"AI 引导的目标替换"必须严格区分
- 案例说明「用户语气变化」是漂移的早期信号之一（FocusLoop 暂未设计此类信号）

---

## 6. 局限性

- 本案例未使用 FocusLoop 实际拦截，仅作为回顾性分析
- 「阶段 4 用户复盘文本」是漂移已发生后的产物，不能据此推断 FocusLoop 在阶段 2-3 时是否能给出有意义的提示
- 真实项目使用 FocusLoop 的对比数据来自 10 条标注（8 correct、1 false_positive、1 false_negative），不足以证明该案例对应的提示都能被正确触发

---

## 7. 来源

- 阶段 1-4 对话记录
- 阶段 5 对话记录