# FocusLoop 漂移判定标准 v0.3

状态：v0.3 更新（2026-09-21），引用 C5 案例作为「合法演进 vs AI 引导替换」区分的真实证据

---

## 打断触发条件

### 场景 A：用户主动触发（高优先级）
当用户在单个项目中提出路线确认请求 ≥ 2 次，立即检测当前上下文是否存在漂移，并打断一次。

### 场景 B：系统自动触发（可配置）
当检测到高置信度偏移时，在当前工作段完成后打断一次。提醒模式三种：
- 每轮打断：每次检测到高置信度偏移立即打断
- 每3轮打断：累计3次偏移后统一打断一次
- 不再提醒：仅记录，不主动打断

---

## 三类漂移判定闸口

### 闸口 1：新增功能/产出物不符合用户最终需求

**触发条件**：项目新增功能或新增产出物，且该新增内容经 FocusLoop 判断可能不符合用户最初定义的最终需求。

**优先级分层**：

| 类型 | 说明 | 处理优先级 |
|------|------|----------|
| 功能性交付物 | 增加用户需求范围的功能性产出（如新增模块、新平台） | 高优先级触发确认 |
| 证据性交付物 | 服务于目标定义的研究、分析、报告（如案例调研、市场报告） | 低优先级，记录但不立即打断 |

**判断依据**：
- 系统提取新增内容与原始目标的语义重叠度
- 用户在原始目标中明确表达的非目标（non-goals）列表
- 目标契约中的最终交付物定义
- 交付物类型分类：功能性 vs 证据性

**处理流程**：
1. 发现新增功能/产出物 → 分类：功能性 / 证据性
2. 功能性：暂停当前执行，展示原始目标 vs 当前新增内容
3. 用户选择：纳入目标 / 延后 / 放弃

---

### 闸口 2：破坏基础隐私权限或规则

**触发条件**：当前执行任务可能破坏以下任一类别：
- 用户明确声明的数据隐私规则（如"不上传本地文件"）
- 已确认的项目约束条件（如"不使用第三方 API"）
- 安全或权限边界（如"只读文件操作"）

**处理流程**：
1. 检测到潜在违规操作 → 立即暂停
2. 展示：涉及的已确认规则 vs 拟执行操作
3. 用户选择：批准执行 / 取消操作

---

### 闸口 3：实施计划偏离原始路线 ≥ 30%

**触发条件**：实施计划或目的与原始路线重叠度 < 70%。

**排除条件（以下不计入偏离度计算）**：
- 用户明确授权的前置工作阶段（如用户说"先做调研再做主目标"）
- 目标定义文件的正常演化（如 PRD 更新本身不等于漂移）
- 实施顺序的微调（不增加独立阶段）
- 必要排障、正常路径调整、技术选型探索

**计算方式**：
- 实施计划重叠度：当前计划内容中与原始路线重叠的部分 / 当前计划总内容
- 语义判断由 FocusLoop 模型层执行，量化指标为参考值，最终判断由用户确认

**处理流程**：
1. 检测到计划/目的偏离度 ≥ 30%（排除上述情况后）→ 当前工作段完成后打断
2. 展示：原始路线 vs 当前偏离程度（%）
3. 用户选择：调整计划回归 / 更新原始路线 / 继续偏离

---

## 不属于漂移的正常行为

| 行为类型 | 说明 | 是否漂移 |
|---------|------|---------|
| [用户授权的目标改变](02_PRD_草案.md)（C5 阶段 5）| 用户明确表达新的目标方向，并基于已发生的复盘证据 | 否 |
| 目标细化/深化 | 核心方向一致，具体化 | 否 |
| 路径调整 | 遇到障碍换路径，目标不变 | 否 |
| 必要排障 | 排查过程中发现并修复额外问题 | 否 |
| 合理工程补充 | 补充文档、测试、错误处理 | 否 |
| 技术选型探索 | 在原范围内探索不同技术方案 | 否 |
| 基于新信息的合法重新评估 | 有新的、不同的信息支撑 | 否 |
| [用户明确授权的前置工作](02_PRD_草案.md)（C5 阶段 5 案例反推）| 用户主动要求的独立阶段（如调研、案例分析），且发生在复盘后 | 否 |
| 目标定义文件演化 | PRD 等目标文档的正常更新 | 否 |
| 实施顺序微调 | 不增加独立阶段的顺序变化 | 否 |

---

## 目标完整性检查（预防层）

**实现状态：✅ 已完成**。运行 `uv run python shadow/check_goal_completeness.py shadow/focusloop_goal.json` 可检查范围、约束、验收、阶段四个维度。

### 背景
闸口只能检测**已定义目标范围内**的漂移。如果目标定义本身遗漏了用户真实意图的某个维度，闸口对此无能为力。这是系统边界，需通过目标定义阶段的完整性检查来补充。

### 触发时机
目标定义完成后、用户确认前，FocusLoop 自动执行完整性检查。

### 检查维度

| 维度 | 检查问题 | 目的 |
|------|---------|------|
| 范围完整性 | 是否有明确的功能边界？有哪些事情明确不做？ | 防止范围模糊导致的隐性漂移 |
| 约束完整性 | 是否有技术约束（如不用某 API）？有隐私/安全要求？ | 支撑闸口 2 判断 |
| 验收完整性 | 如何定义"做完了"？成功标准是什么？ | 防止"做了但不是用户要的" |
| 阶段完整性 | 主要里程碑是什么？哪些阶段必须按顺序？ | 防止阶段顺序漂移 |

### 输出格式
完整性检查后，FocusLoop 向用户呈现：
```
目标完整性检查结果：
[✓] 范围定义：已明确
[✗] 约束条件：未声明"不上传本地文件"类隐私要求
[?] 验收标准：未明确"怎样算完成"
[✓] 阶段定义：已明确

建议补充：
1. 是否有关于数据隐私的约束条件？
2. 如何定义项目成功完成？
```

### 用户操作
- 补充信息：用户补充缺失维度
- 确认忽略：用户认为某维度不适用于当前项目
- 暂不处理：留待后续补充

---

## 约束与非目标列表管理

### 提取方式
系统从对话中自动提取约束条件和用户声明的非目标，提取后提交用户确认。

### 提取触发时机
- 目标定义完成时（目标完整性检查的一部分）
- 用户明确声明限制条件时
- 审查节点（每工作段结束）

### 用户操作
- 确认：纳入约束列表
- 修改：对提取内容提出修改
- 拒绝：标记为误提取

### 约束列表用途
- 闸口 2 的规则判断依据
- 闸口 3 的重叠度计算参照
- 目标完整性检查的约束维度数据源

---

## 实施优先级

| 优先级 | 功能 | 理由 |
|--------|------|------|
| P0 | 闸口 2（隐私/规则破坏） | 信号最清晰，规则层可直接检测，用户最在意 |
| P1 | 约束列表提取+确认 | 其他闸口的基础，需要用户确认流程 |
| P2 | 闸口 1（功能性交付物新增） | 需语义层，但功能性交付物判断相对清晰 |
| P3 | 目标完整性检查 | 预防层，降低闸口盲区的根本手段 |
| P4 | 闸口 1（证据性交付物新增） | 优先级低于功能性，证据性交付物通常不紧急 |
| P5 | 闸口 3（实施计划偏离 30%） | 语义最复杂，且需要闸口 1 的分类能力成熟后才有意义 |
| P6 | 打断频率配置 | ✅ 已实现：`--auto-confirm`、`--reminder-frequency every/every3/never` |
