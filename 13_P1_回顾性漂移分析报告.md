# FocusLoop P1 回顾性漂移分析报告

日期：2026-09-21
方法：对 FocusLoop 开发过程 5 个关键节点，用 FocusLoop 真实工具（`gate3_drift.py check_gate3`）做漂移检查，产出标注案例。

## 节点分析结果

| 节点 | 时间 | 内容 | 人工标签 | FocusLoop 判定 | 重叠度 | 匹配 |
|------|------|------|---------|--------------|--------|------|
| p1-001 | 09-19 上午 | 完成市场调研 → 进入 PRD + 技术方案 | aligned | drift | 0.212 | ❌ |
| p1-002 | 09-19 中午 | 助手提出案例采集，用户要求路线确认（C4） | ambiguous | drift | 0.077 | ❌ |
| p1-003 | 09-19 下午 | Claude Code hooks 验证 → 影子模式实现 | aligned | aligned | 1.000 | ✅ |
| p1-004 | 09-20 下午 | Gate 1/2/3 实现 → 统一 MCP Server | aligned | aligned | 1.000 | ✅ |
| p1-005 | 09-20 晚上 | 多平台适配（Trae + Hermes）| ambiguous | aligned | 1.000 | ❌ |
| p1-006 | 09-21 凌晨 | Trae 中安装 Codex 插件被 Gate 2 阻止 | correct | drift | 0.028 | ❌ |
| p1-007 | 09-21 凌晨 | Trae 中安装 dsh_plugin 被 Gate 2 阻止 | correct | drift | 0.047 | ❌ |

## 根因分析

### False Positive 1：GOAL 缺少前置阶段（p1-001）
- **现象**：市场调研 → PRD → 技术方案，被判为 drift（0.212）
- **根因**：GOAL 的 `deliverables` 列表中未包含"市场调研"作为前置工作，但实际工作流程中它是必要的前置步骤
- **Gap 类型**：目标完整性缺口（预防层缺失）
- **改进方向**：`goal_completeness.py` 应检查 `deliverables` 是否覆盖了所有必要的前置阶段

### False Positive 2：Gate 3 无法捕获元层次问题（p1-002）
- **现象**：助手阶段关系表达不清，用户要求路线确认，被判为 drift（0.077）
- **根因**：Gate 3 检测的是"交付物/实施内容是否在范围内"，不检测"阶段衔接表达是否清晰"
- **Gap 类型**：Gate 3 语义层覆盖范围不足
- **改进方向**：增加元层次检测——当用户提出路线确认请求 ≥ 2 次时，触发阶段关系检查

### False Positive 3 & 4：Gate 3 不检查 non_goals（p1-006, p1-007）
- **现象**：Gate 2 阻止插件安装操作，但 Gate 3 将其判为 drift
- **根因**：`check_gate3` 仅使用 `objective` + `deliverables` + `constraints` 计算重叠度，**完全不看 `non_goals`**
- **Gap 类型**：结构性遗漏——non_goals 是 FocusLoop 目标定义的重要组成部分，但漂移检测完全忽略它
- **改进方向**：`gate3_drift.py` 的 `orig_texts` 应包含 `non_goals`

### 特殊情况：p1-005（判为 aligned，但 human_label 是 ambiguous）
- GOAL 的技术方案部分已记录"多平台适配"方向，因此 aligned 判定符合 GOAL 当前状态
- human_label 标注为 ambiguous 是因为 non_goals 中写了"暂不支持多宿主"，但技术方案已扩展——这是 GOAL 内部矛盾，不应算 FocusLoop 误判

## 结构性问题：Gate 3 不检查 non_goals

这是本次分析发现的最重要结构性 gap：

```python
# gate3_drift.py 当前实现
orig_texts = [orig_o] + orig_d + orig_c  # ❌ 缺少 non_goals
# non_goals（如"暂不支持多宿主"）完全不影响漂移检测
```

**影响**：用户明确写在 non_goals 中的约束，FocusLoop 不会在漂移检测时考虑。这意味着：
- 用户声明"不做 X"，Agent 可以通过"技术方案扩展"绕过
- Gate 2 规则可以阻止，但 Gate 3 漂移检测不会主动提示这种漂移

**修复方案**：
```python
# gate3_drift.py - 修复
orig_ng = goal.get('non_goals', [])
orig_texts = [orig_o] + orig_d + orig_c + orig_ng  # ✅ 包含 non_goals
```

## 更新后定量指标

**数据来源**：`shadow/labels.jsonl`，17 条人工标注（10 条原始 real-xxx + 7 条 P1 节点）

| 指标 | 值 | 说明 |
|------|----|------|
| 总样本数 | 17 | 原始 10 条 + P1 新增 7 条 |
| 准确率 | **64.7%** | 11/17 |
| 误报率 | **29.4%** | 5/17（均已定位根因）|
| 漏报率 | **5.9%** | 1/17（real-009：.git 删除，规则覆盖缺口）|

**误报根因汇总**：
- Gate 3 不检查 non_goals：2 条（p1-006, p1-007）
- GOAL 完整性缺口：2 条（p1-001, real-008）
- Gate 3 语义层覆盖不足：1 条（p1-002）

**改进后预期**：修复 non_goals 检查后，p1-006 和 p1-007 应变为 correct，预期准确率提升至 76.5%（13/17）。

## 与 Baseline 的定性对照

FocusLoop 的核心价值是**主动发现漂移**，Baseline（普通目标提示）完全无法做到以下事情：

| 场景 | Baseline | FocusLoop |
|------|---------|-----------|
| 市场调研完成后进入 PRD | 无感知 | Gate 3 可检测（受 GOAL 完整性影响）|
| 助手提出未确认的阶段变更 | 无感知 | Gate 3 可检测（语义层）|
| Gate 2 阻止操作触发 | 无感知 | Gate 3 可检测 non_goals 漂移（修复后）|
| 用户连续两次要求路线确认 | 无感知 | Gate 3 可触发元层次检查（建议增加）|

FocusLoop 的主动发现能力是 Baseline 无法替代的，但当前 64.7% 的准确率说明判断质量仍需改进。

## 建议

1. **P0 修复**：在 `gate3_drift.py` 的 `orig_texts` 中加入 `non_goals`
2. **P1 修复**：`goal_completeness.py` 增加前置阶段检查
3. **P2 增强**：增加元层次检测（路线确认请求频率）
4. **数据目标**：继续收集标注至 30 条，修复后预期准确率 ≥ 75%、误报率 < 20%
