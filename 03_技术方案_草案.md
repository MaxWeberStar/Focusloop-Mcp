# FocusLoop 技术方案草案 v0.1

状态：Claude Code 宿主接入验证通过；语义纠偏能力与产品化方案仍待评审。依据：02_PRD_草案.md；首版三类问题已获用户认可。

## 1. 关键结论

建议采用 Skill 描述工作规则、本地程序保存状态、单一宿主的 hooks 负责触发检查的组合。首版不必同时开发 MCP 与独立工作台。需要跨客户端提供状态工具时再评估 MCP。

单独提供一个 MCP 工具不保证 Agent 主动调用，也不能因此监听和拦截其他工具。Skill 的文字约定同样不能提供强制执行保证。需要区分建议能力、自动检查能力与执行前介入能力。

## 2. 载体取舍

| 方案 | 可观察范围 | 检查触发 | 暂停能力 | 建议 |
|---|---|---|---|---|
| 纯 Skill | 模型提供的上下文 | 依赖模型遵守规则 | 依赖模型配合 | 可用于早期体验验证 |
| Skill + 本地状态程序 | 上报的计划与产物 | 显式调用 | 单独不能强制阻断 | 最小状态核心 |
| Skill + 状态程序 + 宿主 hooks | 宿主事件实际暴露的内容 | 事件驱动 | 取决于事件、配置与版本 | 推荐验证路线 |
| MCP 工具 | 客户端传入内容 | 通常由模型调用 | 无通用的全局拦截保证 | 跨宿主需求成立后追加 |

Claude Code 2.1.268 已完成真实宿主接入验证，三种事件接收和指定 Write 调用门禁可用，详见第 9 节。结论仅适用于已测试配置与动作，不扩展为全工具覆盖。其他宿主尚未核查，不推断其不支持相同能力。

来源（核查于 2026-09-19）：
- https://code.claude.com/docs/en/hooks
- https://modelcontextprotocol.io/specification/2025-06-18/server/tools

## 3. 最小数据设计

使用项目本地 SQLite 保存状态与历史事件，通过同一程序事务更新，避免状态文件与日志出现两套互相冲突的真相。Markdown 回顾仅为导出视图。

| 对象 | 必要内容 |
|---|---|
| Goal | 项目 ID、版本、目标、交付物、约束、非目标、来源、确认状态 |
| Task | ID、所属目标版本、关联需求、当前阶段、状态 |
| Decision | ID、来源消息、变更内容、用户决定、替代的决定、版本 |
| Observation | 事件 ID、可见计划或行动摘要、证据引用、宿主来源 |
| Review | 检查所用版本、问题类型、判断、证据、建议、处理状态 |

Goal 只有用户确认后才能生效；模型可生成 proposed 项。首次迭代用明确的用户确认操作提交决定。自然语言决定只有从宿主提供的真实用户消息取得且无歧义时才直接记录，不能接受模型自行声称“用户已同意”。对话文字不能替代底层访问控制，因此本系统不作防恶意篡改承诺。

## 4. 检查流程

用户输入或关键计划变化 → 读取当前目标版本 → 收集最小相关证据 → 规则校验 → 必要时语义判断 → 正常继续、进入回顾或显示路线确认卡 → 记录用户决定。

规则负责版本、确认状态、已延后事项和明确约束的一致性；语义判断负责行动与交付目标的关系。模型给出 aligned、suspected_drift 或 unknown，并引用目标项和可见证据，不使用未经校准的概率作为阻断依据。

关键词变化、调用次数增加和新建文件本身均不足以证明偏移。对于必要排障或探索，应允许关联已有任务并给出理由；理由本身也不能被当成独立事实证据。

语义判断先在影子模式记录，验证误报后才考虑启用执行前介入。调用现有宿主判断能力或额外模型服务均是候选，成本和接口验证后再选；没有可用判断器时标记覆盖不足，不能伪装成检查通过。

## 5. 触发与交互

会话开始载入精简目标与当前决定；用户变更需求时更新待决项；准备改变阶段或交付物时检查；阶段结束对比预期产物与证据。普通读取和正常实施不逐次弹窗，也不每个工具调用都重复调用审查模型。

hooks 只能观察其实际提供的事件，不能假定拥有完整意图或全部副作用。复合命令和外部进程可能超出可观察粒度；事后检查不能撤销已发生的行动。

确认卡：原约定、拟变更、证据、影响、用户决定。仅暂停有关动作，宿主若只能阻止单次工具调用，则不得称为暂停整个 Agent。工具权限获批与产品目标变更获批分开记录。

## 6. 可靠性与数据边界

以项目 ID 隔离数据；使用事件 ID 去重；更新时校验目标版本，冲突则重新读取，不覆盖新决定。再次检查已处理问题时复用决定，目标或证据实质变化才重新评估。

模型超时或证据不足产生 unknown 并显示检查降级。普通观察流程可继续；已有待确认范围变更不能因错误自动变成批准。禁止无限重试和每回合重复请求同一确认。

默认仅保留目标、决定和必要证据引用，不导入全部历史会话。额外模型服务若启用，需明确哪些摘要将发送到哪个服务；本地持久化不等于全程本地推理。

## 7. PRD 对应验证

R1：建议不升级为确认；用户确认留下来源；新目标版本覆盖旧目标而保留历史。

R2：三类偏移各有正例；必要探索、正常排障和授权改目标各有反例；信息不足输出 unknown。

R3：只有相关动作被介入；拒绝后不会以重试循环刷屏；确认工具执行不自动确认整个范围变更。

R4：延后决定跨会话保留；回顾区分产物存在与验收通过；旧版本检查不能覆盖当前决定。

开发时采用 Python type hints、snake_case、uv 和 pytest。对状态迁移、版本冲突及去重做确定性测试；语义判断使用保留测试集，比较普通目标提示/阶段总结基线和 FocusLoop。记录召回、误报、打断次数、完成质量、耗时与成本，不预设虚假的改进数字。

## 8. 实施顺序与决策点

1. 评审本方案并选择首个宿主。
2. 用小型接入验证确认事件是否收到、能否在执行前介入、用户决定能否可信回传。失败则明确降级为建议模式，不能隐瞒能力落差。
3. 实现目标与决策状态，连接检查及回顾流程。
4. 影子模式观察误报，与基线对照；达到共同确定的门槛后再启用主动介入。

## 9. Claude Code 接入验证记录

验证目录：`FocusLoop_v1/claude_hook_probe/`。

验证结果：

- Claude Code 版本：2.1.268。
- `UserPromptSubmit`、`PreToolUse`、`PostToolUse` 均收到真实事件。
- `needs_review.txt` 在未批准状态下的 `Write` 被 Hook 拒绝，并返回“FocusLoop 接入测试：该文件需用户批准”。
- 用户通过独立终端执行批准后，重试同一 `Write` 成功，并出现 `PostToolUse`。
- 事件日志记录路径为 `needs_review.txt`，与测试目标一致。

证据：`.probe_data/events.jsonl` 记录了拒绝前后两个 `PreToolUse` 事件；批准后的执行额外产生 `PostToolUse`，文件最终内容为 `hello2`。

能力结论：Claude Code 可以作为 FocusLoop v1 的首个宿主，提供执行前检查和单次工具调用阻断。该验证只证明确定性文件门禁链路可用，不证明目标漂移识别准确，也不证明所有工具、副作用或其他宿主都可拦截。下一步应先实现目标状态与检查结果的最小持久化，再加入影子模式的语义判断。

## 10. 状态与影子模式实现

用户已授权实施下一阶段。实现位于 shadow/：SQLite 目标版本、用户决定、观察和检查记录；CLI 提议/确认/记录/审查/查看；独立的影子事件配置。用户确认通过明确命令提交，Agent 不自动批准真实项目目标。已有门禁探针保持独立。

语义调用选用已安装 Claude Code 的非交互结构化输出，safe-mode 禁用自定义 hooks，工具为空、MCP 隔离、无会话保存、独立临时目录、60 秒超时。复用既有模型与认证配置；仅将目标、有效决定与选定摘要发送到该配置的模型服务。无新凭证或额外供应商配置。CLI 可返回的使用量和费用保留在检查结果；缺失值表示未知。

自动 hooks 当前只采集事件元数据；语义摘要通过 observe 显式提交，review 显式运行。这样可验证判断与持久化，但不宣称已自动理解所有工具行为。元数据不足、输出不合法、引用不存在、超时或状态变化均记 unknown。

最小数据实现暂未建立独立 Task 表：当前判断直接关联目标版本和观察，避免引入未用于验收的任务管理功能。若后续需要任务树，另行设计。所有 Review 均为 shadow，绝不改变目标或输出工具权限决定。

本地测试与模型冒烟记录见 shadow/验证记录.md。真实用户数据效果、与普通提示/总结基线的对照、自动摘要质量仍待验证；当前不启用主动语义阻断。
