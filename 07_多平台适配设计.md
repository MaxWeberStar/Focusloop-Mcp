# Gate 2 多平台适配设计

**日期**：2026-09-20  
**状态**：✅ 架构实现完成；Claude Code、Codex、Trae、Hermes MCP 已验证

## 设计目标

让 FocusLoop Gate 2 规则引擎能在不同 AI Agent 平台上工作：
- 已验证：Claude Code、Codex、Trae、Hermes（stdio MCP）
- 待验证：Workbuddy、DSH Harness

## 架构层次

```
Level 1：平台无关核心（已完成）
├── gate2/checker.py       # Gate2Checker 规则引擎
├── gate2/rules.json       # 静态规则
└── gate2/mcp_server.py    # 通用 MCP Server（stdio）

Level 2：平台适配层（本阶段新增）
├── gate2/adapters/base.py       # PlatformAdapter 抽象基类
├── gate2/adapters/codex.py      # Codex（已验证）
├── gate2/adapters/claude_code.py # Claude Code（已验证）
├── gate2/adapters/trae.py       # Trae（待验证）
├── gate2/adapters/hermes.py     # Hermes（stdio MCP 已验证）
├── gate2/adapters/workbuddy.py  # Workbuddy（待验证）
└── gate2/adapters/dsh_harness.py # DSH Harness（待验证）

Level 3：统一入口
└── gate2/multi_agent_code.py    # 多平台 CLI 调度器
```

## 核心抽象：PlatformAdapter

```python
class PlatformAdapter(ABC):
    @abstractmethod
    def _init_metadata(self) -> AdapterMetadata:
        """返回平台元信息（名称、版本、验证状态等）"""
    
    @abstractmethod
    def generate_mcp_config(self, server: MCPConfig) -> dict:
        """把标准 MCPConfig 转换为该平台专属格式"""
    
    @abstractmethod
    def generate_hook_config(self, hook: HookConfig) -> dict:
        """把标准 HookConfig 转换为该平台专属格式"""
    
    @abstractmethod
    def render_install_files(self, server, hooks=None) -> dict[str, str]:
        """返回 {文件路径: 文件内容}，由调用方写入"""
```

## 各平台验证状态

| 平台 | MCP | Hooks | 状态 | 配置文件 |
|------|-----|-------|------|----------|
| Claude Code | ✅ | ✅ | **已验证** | `.mcp.json`, `.claude/settings.local.json` |
| Codex | ✅ | ❌ | **已验证** | `~/.Codex.json` |
| Trae IDE | ✅ | ❌ | **已验证** | `.trae/mcp.json` |
| Hermes | ✅ | ✅ | **已验证**（stdio MCP + pre_tool_call） | `.hermes/mcp.json`、`.hermes/hooks.yaml` |
| Workbuddy | ❌ | ❌ | 待验证 | 未知 |
| DSH Harness | ❌ | ❌ | 待验证（项目迭代中）| 未知 |

## multi_agent_code CLI

### 列出所有平台

```bash
python3 -m gate2.multi_agent_code list

Platform        Display              Status                MCP Hooks
claude_code     Claude Code          已验证                  ✓   ✓
codex           Codex                已验证                  ✓   ✗
trae            Trae IDE             已验证                  ✓   ✗
hermes          Hermes               已验证                  ✓   ✗
workbuddy       Workbuddy            待验证                  ✗   ✗
dsh_harness     DSH Harness          待验证（项目仍在迭代）  ✗   ✗
```

### 生成配置文件

```bash
python3 -m gate2.multi_agent_code generate claude_code
# 输出 .mcp.json 和 .claude/settings.local.json 的内容
```

### 安装到目标项目

```bash
python3 -m gate2.multi_agent_code install claude_code --target /path/to/project
# 写入 .mcp.json + .claude/settings.local.json

python3 -m gate2.multi_agent_code install claude_code --target . --dry-run
# 只显示将要写入的文件，不实际写入
```

### 验证配置

```bash
python3 -m gate2.multi_agent_code validate claude_code --target .
# 检查配置文件存在、JSON 合法
```

## 待验证平台接入流程

```
1. 获取官方 hook 事件列表
        ↓
2. 模仿 claude_code.py 实现 generate_mcp_config / generate_hook_config
        ↓
3. 修改 AdapterMetadata.supports_mcp/supports_hooks/config_paths
        ↓
4. 在真实环境执行 multi_agent_code install <platform> --target .
        ↓
5. multi_agent_code validate <platform> 验证 JSON 合法
        ↓
6. 真实工具调用测试：触发 FocusLoop Gate 2 检查
        ↓
7. 文档化并把 status 从"待验证"改为"已验证"
```

## 关键设计点

### 1. 单一配置源

`default_focusloop_server()` 和 `default_focusloop_hooks()` 生成标准配置：
- 所有平台共用同一份 MCP server 配置（仅 Python 路径差异）
- Hooks 配置由各平台适配器转换为对应格式

### 2. NotImplementedError 模式

待验证适配器的方法直接 `raise NotImplementedError`，并在方法 docstring 中说明缺失的文档需求。

### 3. Metadata 状态机

```python
class AdapterMetadata:
    status: str = "待验证"  # 待验证 / 已验证 / 实验性 / 已废弃
```

CLI 输出按 status 排序，已验证平台优先。

## 验证测试

| 测试 | 命令 | 期望 |
|------|------|------|
| 平台列表 | `multi_agent_code list` | 显示 6 个平台，状态正确 |
| Claude Code 生成 | `multi_agent_code generate claude_code` | 输出完整 JSON |
| Claude Code 安装 | `install claude_code --target /tmp/x` | 写入 .mcp.json + .claude/settings.local.json |
| 验证 | `validate claude_code --target /tmp/x` | found_files=2, issues=[] |
| Trae 安装 | `install trae --target /tmp/x` | 写入 .trae/mcp.json |
| Hermes | `install hermes --target /tmp/x` | 写入可导入的 `.hermes/mcp.json`；通过 Desktop/CLI 导入后进行真实工具调用 |

## 下一步

1. **补充 Workbuddy**：确认其 MCP 配置路径与 transport 约束
2. **DSH Harness**：跟踪 https://github.com/topics/dsh-plugin 进展
3. **MCP Server 多平台并行运行**：让一个 FocusLoop 实例服务多个平台
